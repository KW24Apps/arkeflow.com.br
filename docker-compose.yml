services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: arkeflow
      POSTGRES_PASSWORD: arkeflow_dev
      POSTGRES_DB: arkeflow_platform
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
